language: python

python:
  - "3.6"

install:
  - apt-get install python3-dev
  - pip install -r requirements.txt
  - pip install pytest && pip install pytest-cov
  - pip install codecov

script:
  - pytest tests/ --cov

after_success:
  - codecov
